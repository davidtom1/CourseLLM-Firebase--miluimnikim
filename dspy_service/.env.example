# ============================================================================
# DSPy Service - Environment Variables Template
# ============================================================================
# Copy this file to .env (or .env.local) in the dspy_service directory.
# This file is safe to commit (contains only placeholders).

# ============================================================================
# LLM Provider Configuration
# ============================================================================
# Choose your LLM provider: "openai" (default, recommended) or "gemini"
LLM_PROVIDER=openai

# Optional: Override the default model for your provider
# For OpenAI: "openai/gpt-4o-mini", "openai/gpt-4o", etc.
# For Gemini: "gemini/gemini-1.5-pro", "gemini/gemini-1.5-flash", etc.
# LLM_MODEL=openai/gpt-4o-mini

# ============================================================================
# API Keys (REQUIRED)
# ============================================================================
# You must set ONE of the following based on your LLM_PROVIDER:

# For OpenAI (required if LLM_PROVIDER=openai)
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-openai-api-key-here

# For Gemini (required if LLM_PROVIDER=gemini)
# Get your API key from: https://aistudio.google.com/app/apikey
# GEMINI_API_KEY=your-gemini-api-key-here
# GOOGLE_API_KEY=your-google-api-key-here

# ============================================================================
# Notes
# ============================================================================
# - The service will fail to start if the required API key is missing
# - Do NOT commit your actual .env file (it's in .gitignore)
# - For local development, OpenAI GPT-4o-mini is recommended (cheap and fast)
